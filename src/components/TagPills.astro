---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-2 text-xs">
  {tags.map((t) => (
    <span class="rounded-full border border-white/10 bg-white/5 px-3 py-1 text-zinc-200">{t}</span>
  ))}
</div>

